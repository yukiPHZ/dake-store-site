/* Phase 1C-C public config. The GA4 Measurement ID is not a secret. */
(function attachDakeStoreMarketObserverConfig(root) {
  "use strict";

  root.DakeStoreMarketObserverConfig = {
    projectId: "dake_store",
    measurementId: "G-VTJKYHD8Q3",
  };
})(typeof window !== "undefined" ? window : globalThis);
